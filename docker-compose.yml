name: meetgeek-temporal

services:
  postgresql:
    image: postgres:16
    environment:
      POSTGRES_USER: temporal
      POSTGRES_PASSWORD: temporal
    volumes:
      - temporal-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U temporal"]
      interval: 5s
      timeout: 5s
      retries: 10

  temporal:
    image: temporalio/auto-setup:1.29.6
    depends_on:
      postgresql:
        condition: service_healthy
    environment:
      DB: postgres12
      DB_PORT: 5432
      POSTGRES_USER: temporal
      POSTGRES_PWD: temporal
      POSTGRES_SEEDS: postgresql
    ports:
      - "7233:7233"

  temporal-ui:
    image: temporalio/ui:2.50.0
    depends_on:
      - temporal
    environment:
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_CORS_ORIGINS: http://localhost:8080
    ports:
      - "8080:8080"

  worker:
    image: meetgeek-temporal-worker
    build:
      context: .
      dockerfile: implementations/temporal/worker/Dockerfile
    depends_on:
      - temporal
    environment:
      TEMPORAL_ADDRESS: temporal:7233
    restart: on-failure

  client:
    image: meetgeek-temporal-client
    build:
      context: .
      dockerfile: implementations/temporal/client/Dockerfile
    depends_on:
      - temporal
      - worker
    environment:
      TEMPORAL_ADDRESS: temporal:7233
    ports:
      - "8000:8000"
    restart: on-failure

volumes:
  temporal-postgres:
